import React from 'react';
import {WebSocketObject} from "../../webSocket/Websocket.types";

interface Props {
  WebSocket: WebSocketObject
}

const Game = ({ WebSocket }: Props): JSX.Element => {
  return <div>:D GAME :D</div>;
};

export default Game;
